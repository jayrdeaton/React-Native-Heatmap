import { HeatmapCalendar } from './components/HeatmapCalendar'

const Heatmap = {
  Calendar: HeatmapCalendar,
}

export default Heatmap
export type { CellMode, ColorScale, DataPoint, HeatmapProps, HeatmapTheme, TooltipData } from './types'
